# Optional: force software rendering (useful for headless / CI environments).
# Copy this file to `.env` and uncomment the lines you need.

# export LIBGL_ALWAYS_SOFTWARE=1
# export MESA_LOADER_DRIVER_OVERRIDE=llvmpipe
# export DISPLAY=
